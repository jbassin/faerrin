{
  "name": "faerrin",
  "private": true,
  "type": "module",
  "workspaces": [
    "pkg/*",
    "pkg/caster/site"
  ],
  "scripts": {
    "test": "bun --filter '*' test",
    "typecheck": "bun --filter '*' typecheck",
    "check": "bun --filter '*' check",
    "build": "bun --filter '*' build",
    "lint": "bun --filter '*' lint",
    "format": "bun --filter '*' format",
    "ci": "dagger call all --source=.",
    "ci:check": "dagger call check --source=.",
    "ci:build": "dagger call build --source=."
  }
}
