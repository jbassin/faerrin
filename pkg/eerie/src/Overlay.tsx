// Phase A placeholder. The real ticker (SSE subscription + rolling list of recent
// rolls + pixi.js crit/fumble fx) lands in Phases C and D.
export function Overlay() {
  return <div className="eerie-overlay" aria-hidden="true" />;
}
